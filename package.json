{
  "name": "spixi-website",
  "version": "1.0.0",
  "scripts": {
    "check-links": "node ./scripts/check-links.js",
    "check-aria": "node ./scripts/check-aria.js"
  },
  "engines": {
    "node": ">=12"
  }
}
